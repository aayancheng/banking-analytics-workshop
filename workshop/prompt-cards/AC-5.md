# Prompt Card AC-5 — Draft the Doc Pack From MY Memos (Session 5)

**This is the real-world pattern: agents draft, humans own accuracy.** Your grade rides on the editing, and the panel will find out if you skipped it.

---

## The brief (copy, attach your four memos, send)

> You are in the `banking-analytics-workshop` repo at `stage-5` (venv at `.venv`). Reviewer and accountable owner: **[YOUR NAME]**.
>
> **Task:** draft my model documentation pack from MY four memos (attached/pasted below) using the structure of `workshop/labs/doc-pack-template.md`.
>
> 1. Draft every section EXCEPT "Limitations & appropriate use" and "Monitoring plan" — leave those as headed placeholders marked `[OWNER WRITES THIS]`. Those are mine.
> 2. Wherever my memos state a number, use it verbatim and add the artifact path that proves it (e.g. `score/docs/validation_report.md`). Where my memos are silent, write `[GAP: not covered in memos]` — do NOT fill gaps from the instructor's pack or from your own inference.
> 3. Where my memo disagrees with a committed artifact, flag it: `[CONFLICT: memo says X, artifact says Y]`. Resolve nothing yourself.
> 4. Output: `my_model_doc.md` at repo root. Then list every `[GAP]` and `[CONFLICT]` you inserted.

---

## Your review checklist (this IS the lab)

- [ ] Spot-check five numbers against their cited artifacts — open the files, don't trust the citation.
- [ ] Every `[GAP]` is real (your memo truly didn't cover it) — fill each yourself or own the gap in the defense.
- [ ] Every `[CONFLICT]` resolved by YOU, with the artifact winning unless you can say why not.
- [ ] Write the two `[OWNER WRITES THIS]` sections yourself, unassisted. The panel asks about exactly these.
