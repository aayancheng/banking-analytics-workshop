---
marp: true
theme: default
paginate: true
style: |
  section { font-size: 26px; }
  section.lead h1 { font-size: 52px; }
  h1 { color: #1a2332; }
  strong { color: #245bb2; }
  blockquote { border-left: 4px solid #245bb2; font-style: normal; }
  table { font-size: 22px; }
---

<!-- _class: lead -->
<!-- Block 0:00–0:10 · Re-verify + recap -->

# S2 — The Score Spine
### a scorecard that passes a hard gate, or doesn't ship

Credit Analytics with AI Agents · Session 2 of 5

```bash
python verify.py        # stage-1 green? you're in the right room
```
Lost since last week? `git checkout stage-1 && python verify.py` — that's the whole recovery.

<!-- Speaker: 2 volunteered homework five-liners ("which app would you trust least?"),
     60 seconds each. Note the answers — S5 panel assignments can mine them. -->

---

<!-- Block 0:10–0:45 · Teach: scorecards -->

# Why banks still run WoE scorecards

Gradient boosting exists. Banks know. They still score with **WoE + logistic**:

- **Adverse action:** the law requires *reasons* — a scorecard's reasons are exact arithmetic, not an approximation of one
- **Effective challenge (SR 11-7):** a validator can re-derive every point by hand
- **Stability:** binned monotonic inputs age gracefully across cycles

> **Interpretability is a regulatory feature, not a performance compromise.**
> Tonight's evidence: the scorecard's AUC will land within 0.01 of the LightGBM challenger you'll meet next week.

<!-- Speaker: this claim is checkable in-repo — scorecard 0.8176 vs LGBM 0.8096.
     The linear-in-WoE model beats the tree model here. Ask why (DGP is linear in
     its drivers; real data is often closer to this than folklore admits). -->

---

# Binning and Weight of Evidence

**Bin** a variable → compare each bin's default odds to the population's:

$$\text{WoE}_\text{bin} = \ln\frac{\%\,\text{goods in bin}}{\%\,\text{bads in bin}}$$

- Positive WoE → safer than average · negative → riskier
- **Monotonic WoE** across bins = the variable tells one coherent story
- **Information Value** ranks variables before any model exists

```
DSCR < 1.0   →  WoE −0.9   (danger)
DSCR 1.0–1.4 →  WoE −0.2
DSCR > 1.4   →  WoE +0.6   (comfort)
```

<!-- Speaker: draw the staircase on the board. IV triage: <0.02 useless, 0.1–0.3
     strong, >0.5 suspicious — "too good" is S1's leakage lesson knocking again. -->

---

# From logistic to 300–850

1. Bin every feature → WoE-encode → logistic regression on WoE
2. Each bin gets **points**; a loan's score = base + sum of its bins' points
3. Scale so 50 points halves the odds (**PDO = 50**) → clamp to 300–850

| Band | Score | Default rate (your data tonight) |
|---|---|---|
| AAA | 750+ | ~2% |
| A | 700–750 | ~3% |
| B | 640–700 | ~4% |
| C | 580–640 | ~7% |
| D | <580 | ~28% |

**Monotonic bands are the sanity check that matters** — a validator reads this table before any AUC.

<!-- Speaker: exact band rates print in the lab's validation report; slide numbers are
     rounded on purpose so students trust their own output over the deck. -->

---

<!-- Block 0:45–1:30 · Lab 2a -->

# Lab 2a — bin and weigh (before the machine does)

**By hand-ish first.** Three variables — `dscr`, `utilization`, `prior_delinquencies`:

1. Cut each into 4–6 bins; compute bin default rates
2. Compute WoE per bin — **is it monotonic? should it be?**
3. Then release the machine: full pipeline on all 15 features

**Three decision points you'll write down** (they go in your memo):
binning granularity · which variable you'd *drop* · what "too good to be true" would look like

<!-- Speaker: the manual bin-first sequence is what makes optbinning feel like a tool
     instead of a magic box. Agent track does the same three decisions as review
     questions on the agent's output. -->

---

<!-- Block 1:40–2:10 · Lab 2b (after break) -->

# Lab 2b — train, evaluate, **GATE**

```bash
make train-score        # trains, evaluates, and asserts the gate
python verify.py        # recomputes AUC independently — track-blind
```

**The gate: held-out AUC ≥ 0.78. Hard assert.**

- Reference build achieved **0.8176 / KS 0.4946** — the bar is real but reachable
- Some configurations in this room **will fail the gate. That is designed.**
- When a gate fails: **investigate the model — never negotiate with the gate**

> Try it: relax the gate in your local `train.py` and re-run `verify.py`.
> The checkpoint doesn't move. The gate you must satisfy is **committed in the tag**.

<!-- Speaker: the planted-fail configs and the catch-a-gate-relaxer moment are in the
     facilitator notes. If someone tries editing verify.py itself: that's tampering
     with the auditor — best class discussion of the night if it happens. -->

---

<!-- Block 2:10–2:40 · Teach: interpretability is the product -->

# Reason codes: the score explains itself

For a linear-in-WoE model, each feature's push on the odds is **exactly**
`WoE(bin) × coefficient`. Top positive pushes = the adverse action letter.

```json
{"feature": "utilization",          "impact": 0.41}
{"feature": "prior_delinquencies",  "impact": 0.33}
{"feature": "dscr",                 "impact": 0.28}
```

**"Defend this scorecard" round:** instructor plays validator. Three students, one question each:
*why is your top variable in the model, and what would make you remove it?*

<!-- Speaker: pick students who made different Lab-2a decisions so the defenses
     differ. Model the tone: a validator is adversarial-curious, not hostile. -->

---

# Checkpoint: stage-2

```bash
python verify.py
```
```
[4/4] Model: scorecard + AUC gate ............ OK
✅ Stage 2 verified — you are here, and it works.
```

**Decision memo (1 page, due S3):**
*My scorecard: what's in it, what I excluded and why, and the one question I'd fear from a validator.*

Next week your score starts making **money decisions** — and finds $1.28B of mispricing.

<!-- Speaker: memos feed the S5 doc pack directly — tell them to keep every memo.
     Verify sweep before dismissal. -->
